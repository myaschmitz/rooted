// This file is kept for backwards compatibility but is no longer needed.
// All alert calls should use the useAlert() hook from contexts/AlertContext.
// The AlertProvider renders a custom modal that works consistently across all platforms.

export { useAlert } from '../contexts/AlertContext';
